# MathAI v0.7.0 資料架構

## 原則

### 1. 會員資料只有一份
`student_profile_controls`

保存：
- Email
- 姓名鎖定
- 縣市／區域／學校
- 年級／版本
- 學習狀況
- 興趣
- 得知 MathAI 的來源
- 年級／版本修改次數
- 管理員指定推薦資格

`user_profiles` 保留作舊資料，不再由 v0.7.0 App 使用。

### 2. 點數只有一份
`member_wallets`

所有正式加扣點都必須透過資料庫 RPC。
Session State 只保存「目前畫面要顯示的鏡像值」。

### 3. 點數異動一定留流水
`member_credit_ledger`

可追查：
- 出題扣點
- AI 失敗退款
- 回饋獎勵
- 優惠碼
- 推薦獎勵
- 管理員調整

### 4. 推薦與使用紀錄分離
`referrals`：推薦關係與獎勵狀態  
`user_activity_events`：成功使用 AI 功能的紀錄

### 5. 登入資料流

Email OTP 成功
→ 從 `student_profile_controls` 讀會員資料
→ 從 `member_wallets` 讀點數
→ 放進 Session 顯示
→ Session 不得反向用舊值覆蓋雲端

## 後續建議

v0.7.x 之後再逐步整理：
- 試卷保存到 Supabase
- OTP／登入持久化
- 儲值申請與管理員審核 RPC 化
- 把超大型 `app.py` 拆成資料層、服務層、介面層

本版先完成最容易造成資料錯亂的三個核心：會員、點數、推薦。
